<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Mux Player offline downloads =====
         Only required if the app uses Mux Player's offline download support. The mux-player library
         intentionally does not declare these, since they are sensitive and shouldn't be forced on
         apps that don't download media. See the matching <service> block below in <application>. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <!-- Required for DownloadService progress notifications on API 33+ (also request at runtime) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Lets PlatformScheduler restart downloads after a device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- ===== end Mux Player offline downloads ===== -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MuxVideoMedia3"
        >
        <activity
            android:name=".examples.carousel.PlayerCarouselActivity"
            android:screenOrientation="sensorPortrait"
            android:exported="false"
              android:theme="@style/Theme.MuxVideoMedia3.NoActionBar"
            tools:ignore="LockedOrientationActivity" />
        <activity
          android:name=".examples.BasicPlayerActivity"
          android:exported="false"
          android:label="Basic Foreground Player" />
        <activity
            android:name=".examples.ConfigurablePlayerActivity"
            android:exported="false"
            android:label="Basic Foreground Player" />
        <activity
            android:name=".examples.SmartCacheActivity"
            android:exported="false"
            android:label="Smart Caching Example" />
        <activity
            android:name=".examples.offline.OfflineDownloadsActivity"
            android:exported="false"
            android:label="Offline Downloads Example"
            android:theme="@style/Theme.MuxVideoMedia3.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MuxVideoMedia3.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== Mux Player offline downloads =====
             Only required if the app uses Mux Player's offline download support. Paired with the
             permissions declared above at the top of the manifest. -->
        <service
            android:name="com.mux.player.offline.MuxDownloadService"
            android:foregroundServiceType="dataSync"
            android:exported="false">
            <intent-filter>
                <action android:name="androidx.media3.exoplayer.downloadService.action.RESTART"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>
        <!-- JobService used by PlatformScheduler (MuxDownloadService.getScheduler()) to reschedule
             downloads when their requirements are met and after a reboot -->
        <service
            android:name="androidx.media3.exoplayer.scheduler.PlatformScheduler$PlatformSchedulerService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="true"/>
        <!-- ===== end Mux Player offline downloads ===== -->
    </application>

</manifest>